#include <iostream>

int get_change(int m) {
  std::vector<int> MinNumCoins(m);
  MinNumCoins(0)=0
for(int m=1;m<=m;m++)
MinNumCoins(m)=
for i from 1 to |coins|:
if m = coini:
NumCoins=MinNumCoins(m - coini) + 1
if NumCoins=MinNumCoins(m):
MinNumCoins(m)=NumCoins
return MinNumCoins(money)
  
}

int main() {
  int m;
  std::cin >> m;
  std::cout << get_change(m) << '\n';
}
